salario_hora = float(input("Digite o valor do salário/hora:\n"))
horas_trabalhadas = float(input("Digite a quantidade de horas trabalhadas:\n"))

salario = salario_hora * horas_trabalhadas

print(f"O valor do salário é {salario}")